<%- include('../partials/head') %>
    <%- include('../partials/nav') %>
    <% if(admin){%>
        <% if(revista.length> 0){ %>
            <a href="/add_Revista" class="btn-plus"><i class="fa-solid fa-circle-plus"></i></a>

            <div class="container table-responsive tbm animate__animated animate__bounce">
                <table class="table table-light table-bordered table-hover">
                    <thead>
                        <tr class="table-dark">
                            <th>N#</th>
                            <th>Titulo</th>
                            <th>Autor</th>
                            <th>Fecha</th>
                            <th>Recurso</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i=0; i < revista.length; i++){ %>
                            <tr>
                                <td>
                                    <%= i+1 %>
                                </td>
                                <td>
                                    <%= revista[i].titulo %>
                                </td>
                                <td>
                                    <%= revista[i].autor %>
                                </td>
                                <td>
                                    <%= revista[i].fechaPublicacion %>
                                </td>
                                <td>
                                    <img src="<%= revista[i].img %>" class="img-tabla">
                                </td>
                                <td>
                                    <a href="/delete_revista/<%=revista[i].idRevista%>" class="btn btn-danger"><i class="fa-solid fa-trash-can"></i></a>
                                    <a href="/edit_revista/<%=revista[i].idRevista%>" class="btn btn-info"><i class="fa-solid fa-pencil"></i></a>
                                </td>
                            </tr>
                            <% } %>
                    </tbody>
                </table>
            </div>
            <% } else{ %>
                <div class="container cont animate__animated animate__flipInY">
                    <div class="card">
                        <div class="card-body">
                            <h1>No hay Libros</h1>
                        </div>
                    </div>
                </div>
                <% } %>
    <%}else{%>
        <div class="container">
            <div class="row">
                <% for(var i=0; i < revista.length; i++){ %>
                    <div class="card col-12 col-md-3 col-sm-12 tarj animate__animated animate__backInDown">
                        <div class="card-header title-Tarj">
                            <h4><%= revista[i].titulo%></h4>
                        </div>
                      <div class="card-body">
                        <center>
                            <img src="<%= revista[i].img%>" class="img-tarj">
                          </center>
                      <hr>
                      <p>
                        <b>Autor:</b> <%= revista[i].autor%><br/>
                        <b>Fecha:</b> <%= revista[i].fechaPublicacion%>
                        <a href="<%= revista[i].img%>" target="_blank" class="btn btn-info">
                            <i class="fa-sharp fa-solid fa-eye"></i>
                        </a>
                        </p>
                      </div>
                    </div>
                <%}%>
            </div>
        </div>
    <%}%>
<%- include('../partials/footer') %>